#include "Hooks.h"

namespace Hooks
{
	/*bool HookActorEquipManager::EquipObject(
		RE::ActorEquipManager* a_self,
		RE::Actor* a_actor,
		const RE::BGSObjectInstance& a_object,
		std::uint32_t a_stackID,
		std::uint32_t a_number,
		const RE::BGSEquipSlot* a_slot,
		bool a_queueEquip,
		bool a_forceEquip,
		bool a_playSounds,
		bool a_applyNow,
		bool a_locked)
	{
		return HookActorEquipManager::EquipObjectHook01(a_self, a_actor, a_object, a_stackID, a_number, a_slot, a_queueEquip, a_forceEquip, a_playSounds, a_applyNow, a_locked);
	}

	void HookAIProcess::SetEquippedItem(
		RE::AIProcess* a_self,
		RE::Actor* a_actor,
		const RE::BGSObjectInstance& a_instance,
		const RE::BGSEquipSlot* a_slot)
	{
		REX::INFO(std::format("SetEquippedItem {0} {1} {2} {3}.", a_self != NULL, a_actor != NULL, a_instance.object != NULL, a_slot != NULL));
	}*/
}
